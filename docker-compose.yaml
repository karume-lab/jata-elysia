services:
  postgres:
    image: postgres:17-alpine
    container_name: jata-elysia-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: hello
      POSTGRES_DB: todo_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 5s
      retries: 5
      start_period: 5s

  pgadmin:
    image: dpage/pgadmin4:8.14
    container_name: jata-elysia-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@mail.com
      PGADMIN_DEFAULT_PASSWORD: hello
    ports:
      - "8080:80"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - pgadmin_data:/var/lib/pgadmin

volumes:
  postgres_data:
  pgadmin_data:
